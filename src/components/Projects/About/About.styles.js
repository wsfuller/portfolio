import { makeStyles } from '@material-ui/core/styles';

const useAboutStyles = makeStyles(theme => ({
  root: {
    marginBottom: theme.pxToRem(theme.spacing(4))
  }
}));

export default useAboutStyles;

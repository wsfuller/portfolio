import { makeStyles } from '@material-ui/core/styles';

const SocialNetworkStyles = makeStyles(theme => ({
  container: {
    marginTop: theme.pxToRem(16)
  }
}));

export default SocialNetworkStyles;
